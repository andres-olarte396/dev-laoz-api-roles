# DEPRECATED

Este repositorio está deprecado y ya no se mantiene activamente.

La funcionalidad de gestión de roles ha sido absorbida en:

**→ [dev-laoz-authentication-api](https://github.com/andres-olarte396/dev-laoz-authentication-api)**

El servicio canónico expone:
- `/api/auth` — autenticación (login, refresh, logout, verify)
- `/api/roles` — gestión de roles y permisos (CRUD + verificación interna)
